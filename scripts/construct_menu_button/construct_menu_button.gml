///@construct_menu_button()
function construct_menu_button(_name, _value) constructor {
	//we can use recursion to create additional menu buttons [for options, graphics, sound buttons etc]
	name = typecase(_name);
	value = _value;
}