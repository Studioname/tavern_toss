///@create_objects_from_grid_data()
function create_objects_from_grid_data(){
	//we need to create another struct containin non-essential furniture pieces
	//like tables, chairs, plant pots, barrels etc
	//we then use a constructor and assign instance ids to grid cells
}